# ⚡ БЫСТРЫЙ СТАРТ

## 🎯 Live Grid - Платформа для поиска недвижимости

---

## 🚀 Запуск (3 шага)

### 1️⃣ Dev сервер уже запущен!

```
✅ Vite dev server: http://localhost:3000
✅ HMR активен
✅ Без ошибок
```

**Откройте браузер**: http://localhost:3000

---

### 2️⃣ Если нужно перезапустить

```bash
# Остановить (Ctrl+C)
# Запустить снова:
npm run dev
```

---

### 3️⃣ Production сборка

```bash
npm run build
npm run preview
```

---

## 📁 Структура (где что лежит)

```
src/
├── components/
│   ├── layout/           ← Header, Footer
│   ├── sections/         ← 6 секций страницы
│   └── ui/               ← CategoryCard, PropertyCard, etc.
├── assets/
│   ├── images/           ← 18 изображений
│   └── icons/            ← 5 иконок
├── data/                 ← mockData.js (тестовые данные)
├── utils/                ← helpers.js (утилиты)
└── constants/            ← theme.js (цвета, шрифты)
```

---

## 🎨 Что на странице

### Сверху вниз:

1. **Header** - Шапка с навигацией
2. **Search Section** - Поиск + 10 категорий
3. **Offers Section** - 6 новых объявлений + баннер
4. **ЖК Section** - 2 карточки жилых комплексов
5. **Hot Offers** - 3 горящих предложения
6. **Launch Sales** - 3 старт продаж
7. **About Platform** - О платформе (статистика)
8. **Footer** - Подвал с ссылками

---

## 🔧 Как изменить

### Цвета
Файл: `tailwind.config.js`
```js
colors: {
  primary: '#3CA4F4',  // ← Основной цвет
  ...
}
```

### Тестовые данные
Файл: `src/data/mockData.js`
```js
export const propertyListings = [...]
export const residentialComplexes = [...]
```

### Добавить новую секцию
1. Создать файл: `src/components/sections/NewSection.jsx`
2. Импортировать в `src/App.jsx`
3. Добавить `<NewSection />` в разметку

---

## 📱 Адаптивность

Проверить на разных размерах:

- **Desktop**: 1200px+ (полная версия)
- **Tablet**: 768-1024px (средняя)
- **Mobile**: 320-768px (мобильная)

Все работает автоматически через Tailwind классы:
- `lg:...` - для desktop
- `md:...` - для tablet
- По умолчанию - для mobile

---

## ❗ Важно

### Изображения
- Все изображения в `src/assets/images/`
- НЕ удалять, они используются в компонентах
- Формат: PNG @2x для качества

### Иконки
- Все иконки в `src/assets/icons/`
- Формат: SVG для масштабируемости

### Шрифт
- Rubik подключен через Google Fonts
- Прописан в `index.html`
- Используется во всех компонентах

---

## 🐛 Если что-то не работает

### 1. Dev сервер не запускается
```bash
rm -rf node_modules
npm install
npm run dev
```

### 2. Изображения не загружаются
Проверьте пути импорта в компонентах:
```js
import logo from '../../assets/images/logo-98ba9f.png'
```

### 3. Стили не применяются
Проверьте, что Tailwind работает:
```bash
npm run dev
# Должен показать: "✓ built in XXXms"
```

---

## 📚 Документация

- **README.md** - Полная документация проекта
- **REPORT.md** - Детальный отчет о работе
- **CHANGELOG.md** - История изменений
- **PROJECT_SUMMARY.md** - Краткая сводка

---

## ✨ Готово!

Проект готов к использованию.  
Все компоненты работают.  
Верстка pixel-perfect соответствует Figma.

**Приятной разработки!** 🚀

---

**Вопросы?**  
Читайте README.md для деталей.
