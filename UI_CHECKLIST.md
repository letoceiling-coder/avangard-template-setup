# Чек-лист единообразия UI-компонентов

## ✅ Что уже сделано

### 1. Созданы базовые UI-компоненты
- ✅ `Button.jsx` - единый компонент кнопки с вариантами (primary, secondary, outline, ghost, danger)
- ✅ `Input.jsx` - единый компонент поля ввода
- ✅ `Tab.jsx` - единый компонент переключателя/таба
- ✅ `IconButton.jsx` - единый компонент иконочной кнопки
- ✅ `index.js` - единая точка экспорта всех UI-компонентов

### 2. Обновлены компоненты для использования единой системы

#### Layout компоненты
- ✅ `Header.jsx` - использует Button и IconButton

#### Sections компоненты
- ✅ `SearchSection.jsx` - использует Button, Input, Tab, TabGroup, IconButton
- ✅ `ResidentialComplexSection.jsx` - использует Button
- ✅ `QuizSection.jsx` - использует Button
- ✅ `OffersSection.jsx` - использует Button

#### UI компоненты
- ✅ `PropertyCard.jsx` - использует IconButton для избранного
- ✅ `ResidentialComplexCard.jsx` - использует IconButton для избранного
- ✅ `FilterPanel.jsx` - использует Button и Input

#### Pages
- ✅ `CatalogPage.jsx` - использует Button
- ✅ `PropertyDetailPage.jsx` - использует Button и Input
- ✅ `NewBuildingDetailPage.jsx` - использует Button

---

## 📋 Единые параметры

### Размеры элементов
Все компоненты используют только 3 размера:

| Размер | Высота | Padding | Font Size |
|--------|--------|---------|-----------|
| **sm** | 40px   | 16px    | 13px      |
| **md** | 48px   | 24px    | 14px      |
| **lg** | 56px   | 32px    | 16px      |

### Радиусы скругления
- Кнопки/инпуты: **8px** (основной)
- Малые элементы: **6px**
- Карточки: **16px**
- Модальные окна: **12px**
- Бейджи/теги: **25px** (rounded-full для pill-shape)

### Цвета
```js
Primary: #3CA4F4
Primary Dark: #2A8FD9 (hover)
Dark: #1E1E1E (текст)
Gray Light: #DFDFDF (рамки)
Gray Medium: #8E8E8E (второстепенный текст)
White: #FFFFFF
```

---

## 🎯 Правила использования

### ✅ ПРАВИЛЬНО

```jsx
// Импорт из единой точки
import { Button, Input, Tab } from '@/components/ui'

// Primary кнопка
<Button variant="primary" size="lg">
  Показать объекты
</Button>

// Secondary кнопка
<Button variant="secondary" size="md">
  Войти
</Button>

// Поле ввода с иконкой
<Input
  placeholder="Поиск"
  icon={<SearchIcon />}
  size="lg"
/>

// Группа табов
<TabGroup>
  <Tab active={tab === 0}>Квартиры</Tab>
  <Tab active={tab === 1}>Дома</Tab>
</TabGroup>
```

### ❌ НЕПРАВИЛЬНО

```jsx
// НЕ создавать inline-кнопки с разными стилями
<button className="px-5 py-2.5 bg-primary rounded-[5px]">
  Кнопка 1
</button>

<button className="px-6 py-3 bg-primary rounded-[8px]">
  Кнопка 2  // РАЗНЫЕ РАЗМЕРЫ!
</button>

// НЕ использовать разные радиусы
<button className="px-4 py-2 rounded-[10px]">...</button>
<button className="px-4 py-2 rounded-[5px]">...</button>
```

---

## 🔍 Как проверить консистентность

### 1. Проверка размеров
```bash
# Найти все inline-кнопки с разными padding
rg "px-\d+ py-\d+" --type jsx
```

### 2. Проверка радиусов
```bash
# Найти все элементы с нестандартными радиусами
rg "rounded-\[(?!8px|6px|16px|12px|25px)" --type jsx
```

### 3. Визуальная проверка
- Открыть страницу в браузере
- Проверить, что все primary-кнопки выглядят одинаково
- Проверить, что все secondary-кнопки выглядят одинаково
- Проверить, что все поля ввода имеют одинаковую высоту и стиль

---

## 📝 Контрольные вопросы перед коммитом

- [ ] Используются ли единые компоненты `Button`, `Input`, `Tab` вместо inline-элементов?
- [ ] Все кнопки одного типа выглядят одинаково?
- [ ] Используются только стандартные размеры (`sm`, `md`, `lg`)?
- [ ] Радиусы скругления единообразны (8px для кнопок/инпутов)?
- [ ] Цвета соответствуют теме (`theme.js`)?
- [ ] Hover-эффекты одинаковы для похожих элементов?
- [ ] Transitions везде `duration-300`?

---

## 🚀 Следующие шаги

1. **Для новых компонентов:**
   - Всегда использовать компоненты из `@/components/ui`
   - Не создавать inline-кнопки и поля
   - Следовать документации из `UI_SYSTEM.md`

2. **При обновлении существующих компонентов:**
   - Заменить inline-элементы на единые компоненты
   - Проверить размеры и отступы
   - Убедиться в консистентности с остальным UI

3. **Регулярная проверка:**
   - Запускать `npm run build` для проверки ошибок
   - Визуально проверять страницы на консистентность
   - Использовать grep для поиска нестандартных стилей

---

## 📚 Полезные ссылки

- `UI_SYSTEM.md` - Полная документация по системе UI-компонентов
- `src/constants/theme.js` - Константы цветов, размеров, шрифтов
- `src/components/ui/` - Директория с UI-компонентами
- `tailwind.config.js` - Конфигурация Tailwind CSS

---

**Помните:** Консистентность UI = Профессиональный вид проекта = Доверие пользователей
