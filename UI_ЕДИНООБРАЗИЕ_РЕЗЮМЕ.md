# Единообразие UI - Краткое резюме

## 🎯 Что сделано

Создана **единая система UI-компонентов** для обеспечения консистентности интерфейса во всем проекте LiveGrid.

## 📦 Новые компоненты

В папке `src/components/ui/` созданы:

1. **Button.jsx** - универсальная кнопка
2. **Input.jsx** - универсальное поле ввода
3. **Tab.jsx** - переключатель/таб
4. **IconButton.jsx** - кнопка с иконкой
5. **index.js** - единая точка экспорта

## ✨ Единые параметры

### Размеры (используйте ТОЛЬКО эти):
- **sm** → высота 40px, текст 13px
- **md** → высота 48px, текст 14px ⭐ по умолчанию
- **lg** → высота 56px, текст 16px

### Радиусы скругления:
- **8px** для кнопок и полей ввода
- **6px** для малых элементов
- **16px** для карточек

### Цвета:
- **Primary**: `#3CA4F4` (синий)
- **Dark**: `#1E1E1E` (текст)
- **Gray**: `#DFDFDF` (рамки)

## 💡 Как использовать

### Простой пример:
```jsx
import { Button, Input, Tab } from '@/components/ui'

// Primary кнопка
<Button variant="primary" size="lg">
  Показать объекты
</Button>

// Secondary кнопка
<Button variant="secondary" size="md">
  Войти
</Button>

// Поле поиска
<Input 
  placeholder="Поиск"
  icon={<SearchIcon />}
  size="lg"
/>

// Табы
<Tab active={true}>Квартиры</Tab>
```

## 📋 Обновленные файлы

Все основные компоненты обновлены для использования единой системы:

### Layout
- ✅ Header.jsx

### Sections
- ✅ SearchSection.jsx
- ✅ ResidentialComplexSection.jsx
- ✅ QuizSection.jsx
- ✅ OffersSection.jsx

### UI Components
- ✅ PropertyCard.jsx
- ✅ ResidentialComplexCard.jsx
- ✅ FilterPanel.jsx

### Pages
- ✅ CatalogPage.jsx
- ✅ PropertyDetailPage.jsx
- ✅ NewBuildingDetailPage.jsx

## 🚫 Что НЕ делать

### ❌ НЕПРАВИЛЬНО:
```jsx
// Разные размеры для похожих кнопок
<button className="px-5 py-2.5 bg-primary rounded-[5px]">
  Кнопка 1
</button>
<button className="px-6 py-3 bg-primary rounded-[8px]">
  Кнопка 2
</button>

// Разные радиусы
<button className="rounded-[10px]">...</button>
<button className="rounded-[5px]">...</button>
```

### ✅ ПРАВИЛЬНО:
```jsx
// Единые компоненты с единым стилем
<Button variant="primary" size="md">
  Кнопка 1
</Button>
<Button variant="primary" size="md">
  Кнопка 2
</Button>
```

## 📚 Документация

Полная документация находится в:

1. **UI_SYSTEM.md** - подробное описание всех компонентов
2. **UI_CHECKLIST.md** - чек-лист для проверки
3. **MIGRATION_COMPLETE.md** - отчет о миграции

## 🔍 Проверка

Проект успешно собирается:
```bash
npm run build
# ✅ Сборка успешна без ошибок
```

## 🎨 Варианты кнопок

### Button variants:
- `primary` - синяя (основные действия)
- `secondary` - белая с рамкой (второстепенные)
- `outline` - прозрачная с рамкой
- `ghost` - прозрачная без рамки
- `danger` - красная (удаление)

### Примеры:
```jsx
<Button variant="primary">Применить</Button>
<Button variant="secondary">Отмена</Button>
<Button variant="outline">Сбросить</Button>
<Button variant="ghost">Закрыть</Button>
<Button variant="danger">Удалить</Button>
```

## 🎯 Главное правило

> **Все одинаковые по сути элементы должны выглядеть одинаково!**

- Все primary-кнопки → один размер, один стиль
- Все поля ввода → одна высота, один стиль
- Все табы → единые стили active/inactive

## 🚀 Начало работы

1. Импортируйте компоненты:
```jsx
import { Button, Input, Tab, IconButton } from '@/components/ui'
```

2. Используйте их везде вместо inline-элементов

3. Всегда указывайте:
   - `variant` для кнопок (primary/secondary/etc)
   - `size` (sm/md/lg)

4. При сомнениях смотрите примеры в обновленных компонентах

## ✅ Проверка консистентности

Перед коммитом убедитесь:
- [ ] Используются компоненты из `@/components/ui`
- [ ] Не создаются inline-кнопки
- [ ] Все размеры стандартные (sm/md/lg)
- [ ] Радиусы единообразны (8px)

---

## 🎉 Результат

Теперь ваш проект имеет:
- ✅ Единообразный внешний вид
- ✅ Легкую поддержку (меняем 1 файл → обновляются все кнопки)
- ✅ Быструю разработку (готовые компоненты)
- ✅ Профессиональный UI

**Вопросы?** → Смотрите `UI_SYSTEM.md` для полной документации
